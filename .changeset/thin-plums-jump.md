---
"@forthtilliath/react-native-kit": minor
---

Add `useEffectiveColorScheme`, `ThemeToggle`, `useUpdateCheck`, and `UpdateAvailableBanner` — light/dark/system theme preference handling, and a generic "check for an update once per launch, notify via a dismissible banner" flow with no opinion on where persistence lives.
