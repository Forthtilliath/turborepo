---
"@forthtilliath/react-native-kit": minor
---

Add `Thumbnail`, `SwipeableRow`, `VoiceSearchButton`, `PhotoPicker`, and `PickerModal` — the same themeable-via-`styles`-prop pattern as `ChangelogNotes`, with no dependency on any app's internal theme hook.
