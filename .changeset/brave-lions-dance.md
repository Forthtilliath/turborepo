---
"@forthtilliath/react-native-kit": minor
---

Add `useSubmitGuard`, `useDebouncedChange`, `confirmDestructive`, and framework-agnostic `utils/` (getPeriodStartMs, getMostRecentIds, nextInCycle, normalizeForSearch, rankByNameMatch, escapeCsvField, formatCsvNumber, escapeHtml).
