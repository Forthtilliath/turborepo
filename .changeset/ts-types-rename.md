---
"@forthtilliath/ts-types": minor
---

First release. Renamed from `@forthtilliath/types` — leaves room for future per-language type packages (e.g. Java/Angular) instead of one generic `types` name. No code changes.
